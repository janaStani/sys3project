VITE_API_URL=http://localhost:30100

# vite dev server on port 3000 (live reloading frontend)
# frontend and backend run on different ports
# the frontend needs the full backend URL to find it
# used by npm run dev